import {createCounter} from './createCounter.js'

const counter_0 = createCounter()
const counter_1 = createCounter(undefined, undefined, 5)
const counter_2 = createCounter(3, 5)
const counter_3 = createCounter(123)
const counter_4 = createCounter(10, 5, 15)




